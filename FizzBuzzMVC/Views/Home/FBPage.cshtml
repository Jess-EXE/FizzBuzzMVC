@model FizzBuzzMVC.Models.FizzBuzz;



<div class="container col-xxl-8 px-4 py-5 mt-5">
    <div class="row gy-2 h-100 row-cols-md-2 row-cols-1">
        <div class="col border-2 border-end">
            <h1 class="fw-bold lh-1 mb-3">FizzBuzz</h1>
            <h4 class="lead">
                This appilication will take the user's input and then it will use conditional logic based on what is entered on numbers 1 - 100 inclusive.
                It will print FIZZ for the numbers that are divisible by the first number. BUZZ will be printed for the numbers that are divisible by
                the second number. It will print FIZZBUZZ for the numbers that are divisible by both. If none of those conditions are met, it will simply
                print the number.
            </h4>

            <h2 class="mt-5 border-bottom border-2">Instructions</h2>
            <p>
                Enter two numbers. The first number you input will be used to evaluate FIZZ, the second will be used to evaulate BUZZ, and the combination
                of the two will be used to evaluate FIZZBUZZ.
            </p>
        </div>
        <div class="col">
            <div>
                <h5>Enter your FIZZ and BUZZ values</h5>
                @* FORM GOES HERE *@
                <form asp-controller="Home" asp-action="FBPage" method="post">
                    <div class="form-group">
                        <label asp-for="FizzValue">Fizz Value</label>
                        <input type="number" asp-for="FizzValue" class="form-control" placeholder="Enter Fizz Value" value="@Model.FizzValue" />
                    </div>
                    <div class="form-group">
                        <label asp-for="BuzzValue">Buzz Value</label>
                        <input type="number" asp-for="BuzzValue" class="form-control" placeholder="Enter Buzz Value" value="@Model.BuzzValue" />
                    </div>
                    <button type="submit" class="btn btn-dark mt-2">Submit</button>
                </form>
                <h2 class="mt-5 border-bottom border-2">Result</h2>
                <div class="table-responsive">
                @* TABLE GOES HERE *@
                @if (@Model.Result.Count > 0)
                    {
                        <table class="table-striped table-hover table table-dark">
                            <thead>
                                <tr><th colspan="5">FizzBuzz Result</th></tr>
                            </thead>
                            @for (int i = 0; i < Model.Result.Count(); i += 5)
                            {
                                <tr><td>@Model.Result[i]</td><td>@Model.Result[i + 1]</td><td>@Model.Result[i + 2]</td><td>@Model.Result[i + 3]</td><td>@Model.Result[i + 4]</td></tr>
                            }

                        </table>

                    }
                </div>
            </div>
        </div>
    </div>
</div>